# 6/30/14

import turtle
al = turtle.Turtle()

rangeOfNumbers = (0, 100)

al.setpos(-100, -100)
x = -95
y = -95

for n in rangeOfNumbers:
	al.forward(50)
	al.left(90)
	
	while (2 > 1):
		al.setpos(x, y)
		x = x + 5
		y = y + 5
	
		if (x == 50):
			break